const { render } = require('node-sass');

{
  ('use strict');

  const select = {
    templateOf: {
      book: '#template-book',
    },
    containerOf: {
      booksList: '.books-list',
    },
  };
    function render() {
      const thisBooksList = this;
    }
  }
render();
}
